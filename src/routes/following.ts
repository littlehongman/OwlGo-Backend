import { Router } from 'express';

const router = Router();


router.get('/following/:user?', (req, res) => {
    
});

router.put('/following/:user');

router.delete('/following/:user');

export default router
